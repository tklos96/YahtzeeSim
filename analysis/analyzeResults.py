def chi_sq(n,k,actual,expected):
    #calculate chi_squared

with open(logfile) as f:
    lines = f.readlines()

    #Yahtzee analysis
    payoutCounts = [1000, 15, 2]
    chi_sq = chi_sq(n,k,payoutCounts,expectedCounts)

    #FunYahtzee analysis
